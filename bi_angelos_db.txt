1- reservations_2april (
    id INT AUTO_INCREMENT PRIMARY KEY,
    customer_name VARCHAR(100) NOT NULL,
    phone_number VARCHAR(20) NOT NULL,
    reserved_desks TEXT NOT NULL,  -- e.g. "A1, A2, B3"
    remaining DECIMAL(10,2) NOT NULL,      -- how much is still owed
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    pending BOOLEAN DEFAULT 0,
    comment TEXT,
    received_ticket BOOLEAN DEFAULT 0,
    reserved_by VARCHAR(50) DEFAULT NULL
);

3- accounts (
    email VARCHAR(100) NOT NULL UNIQUE,
    role ENUM('Controller', 'Viewer') NOT NULL
);

